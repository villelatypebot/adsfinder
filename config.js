module.exports = {
  // IMPORTANT: You need to replace this with a valid Facebook token
  // The previous token has expired as of May 6, 2025
  // To get a new token:
  // 1. Go to https://developers.facebook.com/tools/explorer/
  // 2. Select your App
  // 3. Add the permissions: ads_read, ads_management
  // 4. Generate Access Token
  // 5. Copy and paste the new token below
  facebookAccessToken: 'REPLACE_WITH_YOUR_NEW_FACEBOOK_ACCESS_TOKEN',
  port: 3000
}; 